"""全局常量配置模块

集中管理所有弹幕渲染、布局、动画和系统相关的常量参数。
修改这些值即可调整弹幕的视觉效果和行为。
"""

import re

# ---------------------------------------------------------------------------
# 礼物常量
# ---------------------------------------------------------------------------
MIN_GIFT_PRICE = 10.0  # 最小礼物价格（元），低于此价格不显示

# ---------------------------------------------------------------------------
# 布局常量
# ---------------------------------------------------------------------------
DANMAKU_X = 30  # 弹幕/渲染层 X 偏移（左侧边距）
BUBBLE_PADDING_X = 14  # 气泡水平内边距
BUBBLE_PADDING_Y = 10  # 气泡垂直内边距
BUBBLE_ROW_GAP = 8  # 气泡内行间距
BUBBLE_VERTICAL_GAP = 8  # 气泡外垂直间距
BUBBLE_MULTILINE_RADIUS = 14.0  # 多行气泡圆角半径
LAYER_WIDTH_EXTRA = 100  # 渲染层额外宽度（防止裁剪）

# ---------------------------------------------------------------------------
# 间距常量
# ---------------------------------------------------------------------------
GIFT_SPACING = 6  # 礼物图片前间距
EMOJI_SPACING = 4  # Emoji 后间距

# ---------------------------------------------------------------------------
# 渲染常量
# ---------------------------------------------------------------------------
FONT_SIZE = 25  # 弹幕字号

# ---------------------------------------------------------------------------
# 布局比例常量
# ---------------------------------------------------------------------------
CONTAINER_BOTTOM_RATIO = 0.98  # 容器底部位置（占视频高度的比例）
MAX_CONTAINER_HEIGHT_RATIO = 0.30  # 最大容器高度比例
MAX_CONTENT_WIDTH_RATIO = 0.8  # 最大内容宽度比例

# ---------------------------------------------------------------------------
# 动画常量
# ---------------------------------------------------------------------------
SPAWN_BACKLOG_DIVISOR = 8  # 积压分流除数（控制每帧生成的弹幕数量）
DAMPING_FACTOR = 0.25  # 平滑阻尼系数（用于弹幕位置缓动）
POSITION_THRESHOLD = 0.1  # 位置吸附阈值（像素）
FADE_OUT_ZONE = 30.0  # 淡出区域高度（像素）

# ---------------------------------------------------------------------------
# 系统常量
# ---------------------------------------------------------------------------
PIPE_BUFFER_SIZE = 10_000_000  # FFmpeg 管道缓冲区大小（字节）
FFMPEG_TIMEOUT = 10  # FFmpeg 进程超时时间（秒）
STDERR_THREAD_TIMEOUT = 5  # stderr 日志线程超时时间（秒）
H264_ALIGNMENT = 16  # H.264 编码对齐粒度（像素）

# ---------------------------------------------------------------------------
# 正则表达式
# ---------------------------------------------------------------------------
# 匹配弹幕文本中的 Emoji 标记，格式为 [emoji_name]
EMOJI_PATTERN = re.compile(r'\[(.*?)]')